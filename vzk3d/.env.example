# Straßenklasse-Backend (US7). Ohne diese Variable nutzt das Frontend direkt
# den manuellen Fallback (keine API-Anfrage).
# Kopiere zu .env und passe die URL an dein laufendes Backend an.
VITE_STREET_CLASS_API=http://localhost:8787/api/street-class
